/* eslint-disable no-unused-vars */
/**
 * Enum for Table names
 * For usage in models migrations seeders
 * @export
 * @enum {number}
 */
// eslint-disable-next-line no-shadow
export enum Tables {
  PEOPLE = 'People',
  HOUSE_HOLDS = 'HouseHolds',
  AUTOS = 'Autos',
  HOUSE_HOLD_OWNERS = 'HouseHoldOwners',
}
